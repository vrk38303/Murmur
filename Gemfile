source 'https://rubygems.org'

# Fastlane handles cert provisioning (match), build & archive (gym), and
# TestFlight upload (pilot). xcpretty makes xcodebuild output readable.
gem 'fastlane', '~> 2.225'
gem 'xcpretty', '~> 0.4'
